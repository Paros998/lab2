server:
  error:
    include-message: always

springdoc:
  swagger-ui:
    path: /swagger-ui.html
  show-login-endpoint: true
  show-actuator: true
logging:
  level:
    root: info
  file:
    name: lab2-app.log